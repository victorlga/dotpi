# Node deps — reinstalled by install.sh on each machine
node_modules/
package-lock.json

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Just in case — never commit secrets or session history
auth.json
sessions/
*.log
